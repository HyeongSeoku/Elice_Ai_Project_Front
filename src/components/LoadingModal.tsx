import loadingSrc from "../img/hash/loading.png";

const LoadingModal = () => {
  return (
    <div className="modalBg">
      <img src={loadingSrc} />
    </div>
  );
};

export default LoadingModal;
